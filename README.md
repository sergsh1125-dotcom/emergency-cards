# ☢️ База Аварійних Карток Небезпечних Хімічних Речовин (НХР)

Це статичний веб-застосунок, розроблений на HTML, CSS та JavaScript, призначений для швидкого пошуку та доступу до 700 аварійних карток НХР у форматі PDF.

## 🚀 Розгортання на GitHub Pages

Цей проєкт ідеально підходить для розгортання через GitHub Pages, оскільки він не вимагає бекенду.

1.  **Створення репозиторію:** Завантажте всі файли (включно з папкою `cards/` та 700 PDF-файлами) до нового репозиторію GitHub.
2.  **Налаштування Pages:**
    * Перейдіть у вкладку **Settings** (Налаштування) вашого репозиторію.
    * Виберіть розділ **Pages** (Сторінки).
    * У секції **Build and deployment** (Складання та розгортання) оберіть `Deploy from a branch` (Розгорнути з гілки) і встановіть гілку `main` (або `master`) та папку `/ (root)`.
    * Натисніть **Save** (Зберегти).
3.  **Доступ:** За кілька хвилин ваш застосунок стане доступним за адресою: `https://<ВАШЕ_ІМ'Я_КОРИСТУВАЧА>.github.io/<ІМ'Я_РЕПОЗИТОРІЮ>/`

## 📋 Вимоги до даних

Для коректної роботи застосунку необхідно дотримуватися такої структури даних:

1.  **Файл `chemical_list.json`:** Має містити повний список 700 речовин з їхніми ID, назвами та точними шляхами до файлів.
2.  **Папка `cards/`:** Усі 700 PDF-файлів повинні бути розміщені в цій папці, а їхні назви повинні **точно** відповідати шляхам, зазначеним у `chemical_list.json` (наприклад, `cards/AK_001_Azotna.pdf`).

## 🔍 Як користуватися

1.  Відкрийте веб-сторінку застосунку в браузері.
2.  **Пошук:** Введіть частину назви або номер картки (наприклад, `001` або `кислота`) у поле пошуку.
3.  **Вибір:** Натисніть на бажану речовину в скрольному списку.
4.  **Результат:** Файл PDF з аварійною карткою автоматично відкриється в новій вкладці або буде завантажений.
